type: custom:hass-app-health-sync-dashboard-card
title: Health
language: auto
days: 7
step_goal: 10000
calorie_goal: 600
show_activity: true
show_sleep: true
show_heart_rate: true

# Optional: select which mobile device this card reads from
# (set in the graphical editor or here via YAML)
# mobile_device: 0123456789abcdef

# Entities are discovered automatically from the HA Companion App.
# Override individual entities here if needed:
# entities:
#   steps: sensor.my_iphone_steps
#   heart_rate: sensor.my_iphone_heart_rate
